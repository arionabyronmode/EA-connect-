name: Auto-RDP with MetaTrader

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */5 * * *'

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Start Message
      run: echo "Starting Windows RDP setup..."

    - name: Install Chocolatey
      shell: powershell
      run: |
        Set-ExecutionPolicy Bypass -Scope Process -Force
        [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
        iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
      continue-on-error: false

    - name: Install MetaTrader 4
      shell: powershell
      run: |
        choco install metatrader4 --yes --ignore-checksums
      continue-on-error: true  # <-- prevents exit code 1 if choco fails

    - name: Start RDP Service
      shell: powershell
      run: |
        try {
          Set-Service -Name TermService -StartupType Automatic
          Start-Service -Name TermService
        } catch {
          Write-Host "RDP service already running or cannot start"
        }

    - name: Keep Session Alive
      shell: powershell
      run: |
        while ($true) {
          Write-Host "RDP session alive..."
          Start-Sleep -Seconds 60
        }
